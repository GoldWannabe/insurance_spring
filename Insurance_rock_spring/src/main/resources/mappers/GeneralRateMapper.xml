<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

	<mapper namespace="GeneralRateMapper">
		<!--  SELECT -->
		<select id="selectAll" resultType="com.mju.spring.Entity.GeneralInsurance">
			SELECT * FROM user
		</select>
	</mapper>